def selectedBranch = env.BRANCH
pipeline {
    agent any

    tools {
        maven '3.8.5'
    }

    stages {
        stage('Maven clean install') {
            steps {
               sh 'mvn -B clean install'
            }
        }
    }
}
