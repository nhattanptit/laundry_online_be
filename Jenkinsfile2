def selectedEnv = $ENVIRONMENT
pipeline {
    agent any

    tools {
        maven '3.8.5'
    }

    stages {
        stage('Maven clean install') {
            steps {
                echo selectedEnv
               sh 'mvn -B clean install'
            }
        }
    }
}
